name: "CD"

on:
  push:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  CD:
    runs-on: ubuntu-latest
    env:
      UNET_DEPLOY: prod

    steps:
    - uses: actions/checkout@v3
    - name: Deploy unet.tech
      run: cargo build --verbose
    - name: Test
      run: cargo test --verbose
